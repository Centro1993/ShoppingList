{
  "name": "shopping-list",
  "version": "1.0.0",
  "description": "A Simple shopping list, made as a learning project with VueJS and NestJS in Typescript.",
  "scripts": {
    "start": "docker-compose build && docker-compose up",
    "start:prod": "docker-compose -f prod.docker-compose.yml build && docker-compose -f prod.docker-compose.yml up -d"
  },
  "author": "Jonas Leitner",
  "license": "ISC"
}
